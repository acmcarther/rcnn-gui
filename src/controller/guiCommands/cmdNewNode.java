package controller.guiCommands;


public class cmdNewNode implements buttonCommand {

	// TODO: Creating a new node
	// 		 Cases: Valid parameters, invalid parameters, pre-existing node
	public void execute() {
		// Update node list
		
		// Compare new node to existing nodes
		
		// If it exists, exit with error
		
		// Check if parent nodes are valid
		
		// Check if child nodes are valid
		
		// If all valid, run http request to add node
		
		// Update node list
		
		// Exit completion
		System.out.println("new node debug");
	}

	

}
