package events.buttons;

public class cmdChgNode implements buttonCommand {
	
	// TODO: Save an existing node
	//       Cases: No node selected, valid parameters, invalid parameters
	//				outdated original node
	public void execute() {
		// If nothing selected, exit with error
		
		// Update node list
		
		// Compare new node to existing nodes
		
		// If it does not exist, exit with error
		
		// Check if parent nodes are valid
		
		// Check if child nodes are valid
		
		// If all valid, run http request to add node
		
		// Exit completion
		
		System.out.println("change node debug");
	}

	

}
