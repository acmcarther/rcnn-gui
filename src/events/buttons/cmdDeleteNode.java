package events.buttons;

public class cmdDeleteNode implements buttonCommand {
	
	// TODO: Delete an existing node
	//		 Cases: No node selected, valid parameter, outdated parameter
	//       Reminder: Delete the entry from the list here or
	//                 -Resolved, list is checked before execution
	public void execute() {
		// If nothing selected, exit with error
		
		// Update node list
		
		// Compare new node to existing nodes
		
		// If it does not exist, exit with error
		
		// If all valid, run http request to delete node
		
		// Exit completion
		System.out.println("delete node debug");
	}
	
}
