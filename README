RCNN - GUI
~~~~~~~~~~~~~~~~~~~~

This is a graphical interface to be used with /RichardKelley/rcnn. It
will eventually have some sort of graphic representation of the network
as well as an interface for working with a running network. This is a 
client for RCNN which runs as a server.

~~~~~~~~~~~~~~~~~~~~

Built in Java using swing libraries.
